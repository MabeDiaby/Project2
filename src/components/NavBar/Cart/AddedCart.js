import React, {useContext} from 'react';
import {CartContext} from '../../../CartContext';

export const AddedCart = () => {
    // const [cart, setCart] = useContext(CartContext);

    return (
        <div>
            {/* <span>items in cart : {cart.length}</span>
            <br />
            <span>total price: $ 0 {cart} </span> */}
        </div>
    )
}